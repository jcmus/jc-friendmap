# 지인 지도 (Friend Map) 프로젝트 계획서

작성일: 2026-08-16

---

## 1. 프로젝트 목표

네이버맵과 같은 지도 위에서 **지인들이 근무하는 회사 위치를 한눈에 파악**하는 지도 앱을 만든다.

- 앱을 켜고 **"내 위치" 버튼**을 누르면 내 현재 위치가 지도에 표시되고, 주변에 지인들이 근무하는 회사가 마커로 표시된다.
- 한국뿐 아니라 **전 세계 어느 나라의 주소든** 동일하게 동작한다.
- 마커를 클릭하면 해당 지인의 **성명, 전화번호, 직책, 이메일, 주소**가 팝업으로 표시된다.

## 2. 확정된 결정 사항

| 항목 | 결정 | 비고 |
|------|------|------|
| 사용 범위 | 1단계는 **혼자 사용**, 이후 공유 앱으로 확장 | 서버 없이 시작 가능 |
| 앱 형태 | **웹앱** (브라우저에서 실행) | PC·스마트폰 모두 사용 가능 |
| 지도 엔진 | **Leaflet + OpenStreetMap** | 완전 무료, API 키 불필요, 전 세계 지원 |
| 지오코딩 (주소→좌표) | **Nominatim** (OSM 무료 서비스) | 전 세계 주소 지원, 초당 1건 제한 |
| 데이터 입력 | **직접 입력 + 엑셀 가져오기** 둘 다 | SheetJS 라이브러리 사용 |
| 데이터 저장 | 브라우저 **localStorage** (1단계) | 엑셀 내보내기로 백업 가능 |

네이버맵 API는 해외 지도 지원이 약해 전 세계 지원 목표에 맞지 않아 제외했다.

## 3. 데이터 모델

지인 1명당 아래 정보를 저장한다.

| 필드 | 설명 | 필수 |
|------|------|------|
| 성명 | 지인 이름 | ✅ |
| 전화번호 | 연락처 | |
| 근무처전화 | 회사 대표/직통 전화 | |
| 직책 | 예: 과장, 팀장 | |
| 부서 | 예: 영업부, 개발팀 | |
| 이메일 | | |
| 회사명 | 근무 중인 회사 | |
| 주소 | 회사 주소 (국내·해외 모두 가능) | ✅ |
| 국가 / 지역 | 시드 데이터의 부가 메타정보 (폼에는 미노출) | |
| 위도/경도 | 주소를 지오코딩해 자동 저장 | 자동 |
| 정밀도 | 좌표를 어떻게 얻었는지 (아래 표 참고) | 자동 |

엑셀 가져오기 양식도 위 열 이름(성명, 전화번호, 직책, 부서, 근무처전화, 이메일, 회사명, 주소)을 그대로 사용한다.

### 좌표 정밀도

주소를 좌표로 바꿀 때 항상 건물 단위로 찾아지는 것은 아니어서, 어떤 근거로 찍힌 위치인지를 함께 저장하고 화면에도 표시한다.

| 값 | 의미 | 화면 표시 |
|----|------|-----------|
| `exact` | 주소 전체가 지도에서 정확히 매칭됨 | (표시 없음) |
| `road` | 세부 도로를 못 찾아 상위 도로 기준으로 찍음 | ≈ 대략적 위치 (도로 기준) |
| `district` | 도로도 못 찾아 구·시 기준으로 찍음 | ≈ 대략적 위치 (구·시 기준) |
| `company` | 주소는 못 찾았지만 같은 회사 동료의 좌표를 물려받음 | ≈ 대략적 위치 (같은 회사 동료 기준) |
| (빈값) | 위치를 전혀 찾지 못함 | ⚠️ 지도 데이터에 이 주소가 없습니다 |

`company`는 회사명의 띄어쓰기와 대소문자를 무시하고 같은 회사로 묶는다. 예를 들어 "도미누스인베스트먼트"와 "도미누스 인베스트먼트"는 동일한 회사로 취급된다.

**기본 시드 데이터**: `data/friends.json`에 미리 채워둔 지인 목록(예: 2,891명)이 있으면 앱 시작 시 자동으로 불러와 병합한다. 이미 저장된 항목(사용자가 수정한 것 포함)은 덮어쓰지 않고, 사용자가 삭제한 시드 항목은 다시 불러오지 않는다. 파일이 없어도 앱은 정상 동작한다.

## 4. 화면 구성

```
┌──────────────┬──────────────────────────────┐
│  사이드바     │                              │
│  ─────────   │                              │
│  📍 내 위치   │         지도 (Leaflet)        │
│  ➕ 지인 추가 │                              │
│  📥📤 엑셀    │    🏢 ← 지인 회사 마커        │
│  🔍 검색      │    (클릭 → 상세 정보 팝업)     │
│  지인 목록    │                              │
└──────────────┴──────────────────────────────┘
```

- **사이드바**: 내 위치 버튼, 지인 추가 버튼, 엑셀 가져오기/내보내기/양식 다운로드, 검색창, 지인 목록
- **지도**: 지인 회사 마커(🏢), 내 위치 표시(파란 점)
- **팝업**: 성명 / 직책·회사명 / 전화(클릭 시 전화걸기) / 이메일(클릭 시 메일쓰기) / 주소 / 수정·삭제 버튼
- **모바일**: 화면이 좁으면 지도 위 + 목록 아래로 자동 전환 (반응형)

## 5. 단계별 개발 계획

### 1단계 — MVP (개인용 웹앱) ✅ 코드 작성 완료

`D:\friend-map\` 폴더에 작성되어 있다.

| 파일 | 역할 |
|------|------|
| `index.html` | 화면 구조 |
| `style.css` | 디자인 |
| `js/store.js` | 데이터 상태·localStorage 저장·시드 병합·CRUD (순수 로직) |
| `js/geocode.js` | Nominatim 지오코딩 (순수 로직) |
| `js/excel.js` | 엑셀 가져오기/내보내기/양식 (SheetJS, 순수 로직) |
| `js/map.js` | Leaflet 지도·마커 클러스터링·내 위치 |
| `js/app.js` | 화면(DOM) 연결 — 위 모듈들을 엮는 유일한 뷰 레이어 |
| `data/friends.json` | 기본 시드 데이터 (있으면 시작 시 자동 병합, 없어도 무방) |
| `server.ps1` | 로컬 실행용 미니 서버 (PowerShell) |
| `지인지도 실행.bat` | **더블클릭 한 번으로 앱 실행** |

2단계부터는 `app.js` 단일 파일 대신 `js/` 폴더 아래 역할별 ES 모듈로 분리했다 (React 포팅을 대비한 구조, 상세는 `REACT-MIGRATION.md` 참고).

포함된 기능:
- 전 세계 지도 표시, 내 위치 이동
- 지인 추가/수정/삭제 (주소 입력 시 자동으로 좌표 변환)
- 마커 클릭 → 성명·전화·직책·이메일·주소 팝업
- 엑셀 양식 다운로드 → 작성 → 한꺼번에 가져오기
- 엑셀 내보내기(백업), 검색, 브라우저 자동 저장

실행 방법: `D:\friend-map\지인지도 실행.bat` 더블클릭 → 브라우저에 `http://localhost:8090` 이 열린다.
(위치 기능은 보안상 localhost 또는 https에서만 동작하므로 이 방식이 필요하다.)

### 2단계 — 개선 ✅ 일부 완료

| 항목 | 상태 | 비고 |
|------|------|------|
| 마커가 많아질 때 자동 묶음 표시 (marker clustering) | ✅ 완료 | Leaflet.markercluster 적용, 2,891건 규모에서도 부드럽게 동작 |
| 기본 시드 데이터 자동 로딩 | ✅ 완료 | `data/friends.json`을 시작 시 병합, 삭제한 시드는 재추가되지 않음 |
| 부서/근무처전화/국가/지역 필드 추가 | ✅ 완료 | 데이터 모델·입력폼·팝업·엑셀 가져오기/내보내기 모두 반영 |
| 사이드바 목록 성능(수천 건) | ✅ 완료 | 검색은 전체 대상, 목록은 150개씩 "더 보기"로 증분 렌더링 |
| React 포팅 대비 모듈 구조화 | ✅ 완료 | `js/store.js`(데이터) · `js/geocode.js` · `js/excel.js` · `js/map.js` · `js/app.js`(DOM)로 분리. 상세 계획은 `REACT-MIGRATION.md` 참고 |
| "내 위치 반경 ○km 안의 지인만 보기" 필터 | ⬜ 미착수 | |
| 지인 사진, 메모 필드 추가 | ⬜ 미착수 | |
| 스마트폰 홈 화면에 앱처럼 설치 (PWA) | ⬜ 미착수 | |
| 명함 CSV 2,891명 시드 데이터 반영 | ✅ 완료 | 주소를 좌표로 변환해 `data/friends.json` 생성. 이 파일은 개인정보라 GitHub에 올리지 않는다 |
| 한국 세부도로 지오코딩 보완 | ✅ 완료 | OSM에 없는 `○○대로NN길` 주소를 상위 도로 → 구·시 순으로 재시도하고, 같은 회사 동료 좌표를 공유 |
| 무료 호스팅(GitHub Pages)에 올려 **내 폰에서도 접속** 가능하게 | 🔶 배포 구성 완료 | 워크플로·가이드 작성 끝. 남은 것은 최초 1회 `git push`와 Pages 설정 → `배포가이드.md` 참고 |
| "내 위치 반경 ○km 안의 지인만 보기" 필터 | ⬜ 미착수 | 2,891명 규모라 실사용 시 가장 먼저 필요해질 기능 |

### 3단계 — 공유 앱 확장 (장기)

지인들도 각자 접속해 자기 근무지를 등록하는 형태.

- 회원가입/로그인 필요 → Firebase 또는 Supabase(무료 시작 가능) 도입
- 데이터가 localStorage → 클라우드 DB로 이동 (1단계 엑셀 내보내기로 데이터 이전 가능)
- 개인정보(전화번호 등) 공개 범위 설정 기능 필수
- 친구 초대/승인 개념 필요

## 6. 주의 사항 및 제약

1. **localStorage의 한계**: 데이터가 해당 PC·해당 브라우저에만 저장된다. 브라우저 데이터를 지우면 사라지므로 **주기적으로 '엑셀 내보내기'로 백업**할 것. (2단계에서 클라우드로 해결)
2. **Nominatim 속도 제한**: 무료 정책상 초당 1건만 지오코딩 가능. 엑셀로 100명을 가져오면 약 2분 소요된다. 정확도가 부족하면 Google Geocoding API로 교체 가능(유료 한도 있음).
3. **주소 정확도**: 해외 주소는 도시·국가까지 포함해 적을수록 정확히 찾는다. 위치를 못 찾은 지인은 목록에 ⚠️ 표시되며 주소를 수정하면 된다.
4. **OpenStreetMap의 한국 주소 공백**: 도로명주소 중 `○○대로27길` 같은 세부 도로가 OSM에 아예 등록되지 않은 경우가 있다. 주소가 정확해도 검색되지 않으므로, 상위 도로나 구·시 기준으로 대략적 위치를 찍고 그 사실을 화면에 표시한다. 건물 단위 정확도가 꼭 필요하면 카카오·네이버 지오코딩 API(국내 도로명주소 지원)로 교체해야 하며, 이 경우 API 키 발급이 필요하다.
5. **개인정보**: 1단계는 내 PC에만 저장되므로 안전하다. 저장소가 공개(public)이므로 `data/`·CSV·엑셀 파일은 `.gitignore`와 배포 워크플로에서 이중으로 차단해 두었다. 3단계 공유 앱으로 갈 때는 지인들의 동의와 공개 범위 설정이 반드시 필요하다.
6. **지오코딩 시 외부로 나가는 정보**: 주소 문자열만 전송된다. 이름·전화번호·이메일·회사명은 외부로 보내지 않는다.

## 7. 다음 할 일

### 내가 직접 해야 하는 것

1. [ ] **GitHub 푸시** — 터미널에서 `git push -u origin main` 실행 (최초 1회 로그인 창이 뜬다)
2. [ ] **GitHub Pages 켜기** — 저장소 Settings → Pages → Source를 `GitHub Actions`로 변경
3. [ ] 폰에서 `https://jcmus.github.io/jc-friendmap/` 접속 → 엑셀 가져오기로 데이터 넣기

### 확인·피드백

4. [ ] `지인지도 실행.bat` 더블클릭으로 앱 실행 확인
5. [ ] 위치를 못 찾은 지인 목록(`data/unresolved.txt`) 훑어보고 주소 보완
6. [ ] 사용해보고 불편한 점 피드백 → 2단계 미착수 항목 우선순위 결정

### 검증 완료 (2026-08-16)

- 시드 데이터 2,891명 자동 로딩, 마커 클러스터링 동작
- 마커/목록 클릭 → 성명·부서·직책·전화·이메일·주소 팝업 (전화·메일 링크 포함)
- 지인 추가 창 열기/닫기 4가지 경로(✕ 버튼·Esc·취소·바깥 클릭)
