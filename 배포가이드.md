# 배포 가이드

작성일: 2026-08-16

이 문서는 지인 지도 앱을 인터넷에 올려 **스마트폰에서도 접속**할 수 있게 만드는 방법을 설명합니다.

---

## 1. 배포 구조 한눈에 보기

```
   내 PC (D:\friend-map)              GitHub 저장소 (공개)           GitHub Pages (https)
┌──────────────────────┐          ┌────────────────────┐        ┌────────────────────┐
│ 앱 코드              │  git push │ 앱 코드만 저장      │  자동   │ 인터넷에 앱 공개    │
│ index.html, js/ ...  │ ────────▶ │ (README, 워크플로)  │ ─────▶ │ 폰·PC 어디서든 접속 │
├──────────────────────┤          └────────────────────┘        └────────────────────┘
│ 🔒 지인 데이터        │  ❌ 올라가지 않음
│ data/friends.json    │     (.gitignore + 배포 화이트리스트로 이중 차단)
└──────────────────────┘
```

**핵심 원칙: 앱(코드)만 인터넷에 올리고, 지인 데이터는 내 기기 안에만 둡니다.**

저장소가 공개(public)로 되어 있어, 지인 2,891명의 이름·전화번호·이메일이 담긴 데이터를 올리면 전 세계 누구나 볼 수 있게 됩니다. 그래서 데이터는 절대 올리지 않는 구조로 설계했습니다.

## 2. 개인정보 이중 안전장치

| 장치 | 위치 | 역할 |
|------|------|------|
| 1차 — 커밋 차단 | `.gitignore` | `data/`, `*.csv`, `*.xlsx`를 git이 아예 추적하지 않음 |
| 2차 — 배포 중단 | `.github/workflows/deploy.yml` 의 "개인정보 파일 검사" | 실수로 데이터가 커밋되면 배포를 실패시킴 |
| 3차 — 화이트리스트 | 같은 파일의 "배포할 앱 파일만 복사" | `index.html`, `style.css`, `js/`만 복사. 나머지는 어떤 경우에도 배포되지 않음 |

## 3. GitHub Pages 켜기 (최초 1회, 웹에서 직접 설정)

코드를 푸시한 뒤, 아래 설정을 한 번만 해주면 이후로는 푸시할 때마다 자동 배포됩니다.

1. https://github.com/jcmus/jc-friendmap/settings/pages 접속
2. **Build and deployment → Source** 를 `GitHub Actions` 로 선택
3. 저장 후 https://github.com/jcmus/jc-friendmap/actions 에서 배포 진행 상황 확인

배포가 끝나면 아래 주소로 접속할 수 있습니다.

```
https://jcmus.github.io/jc-friendmap/
```

## 4. 배포 이후의 사용 흐름

앱 코드만 배포되므로, 각 기기에서 **엑셀 파일로 데이터를 한 번 넣어주면** 그 기기의 브라우저에 저장됩니다.

**PC에서 (데이터 준비)**
1. `지인지도 실행.bat` 실행 → 로컬 앱이 열림 (여기서는 `data/friends.json`이 자동 로딩됨)
2. `📤 엑셀 내보내기` 클릭 → `지인지도_목록.xlsx` 저장
3. 이 파일을 카카오톡·이메일·클라우드 등으로 폰에 전달

**폰에서 (최초 1회)**
1. `https://jcmus.github.io/jc-friendmap/` 접속
2. `📥 엑셀 가져오기` → 전달받은 파일 선택
3. 좌표가 이미 들어 있으므로 지오코딩 없이 즉시 지도에 표시됨
4. 이후에는 접속만 하면 저장된 데이터가 그대로 보임

## 4-1. 앱처럼 설치하기 (PWA)

앱스토어에 올리지 않고도 **홈 화면 아이콘 + 전체화면 실행**이 됩니다. 배포된 주소에 접속한 뒤 홈 화면에 추가하면, 주소창 없이 일반 앱처럼 열립니다.

| | 방법 |
|---|---|
| 아이폰·아이패드 | **사파리**로 접속 → 공유 버튼(⬆️) → "홈 화면에 추가" |
| 안드로이드 | 크롬으로 접속 → 메뉴(⋮) → "앱 설치" 또는 "홈 화면에 추가" |
| PC (크롬·엣지) | 주소창 오른쪽의 설치 아이콘(⊕) 클릭 |

설치하면 이런 점이 달라집니다.

- 홈 화면에 파란 지도핀 아이콘이 생기고, 이름은 "지인 지도"로 표시됩니다
- 주소창·탭 없이 전체화면으로 실행됩니다
- 앱 껍데기(화면·지도 라이브러리)가 기기에 저장돼, 네트워크가 느려도 즉시 뜹니다
- 지인 데이터는 원래 기기 안에만 있으므로 오프라인에서도 목록과 검색이 됩니다
  (단, **지도 배경 타일은 인터넷이 필요**합니다. 오프라인에서는 마커와 정보는 보이지만 지도 그림은 비어 보입니다)

아이폰은 반드시 **사파리**로 접속해야 합니다. 크롬 등 다른 브라우저에서는 "홈 화면에 추가"가 웹 바로가기로만 동작합니다.

앱을 업데이트하면(코드를 고쳐 푸시하면) 설치된 앱도 다음 실행 때 자동으로 새 버전을 받습니다.

**앱스토어에 올리지 않는 이유**: 심사·개발자 등록비(애플 연 $99)·서명 절차가 모두 필요 없고, 개인이 지인들과 쓰기에는 이 방식으로 충분합니다. 다만 앱스토어 검색에는 노출되지 않으므로, 쓸 사람에게 주소를 직접 알려줘야 합니다.

## 5. 코드를 수정한 뒤 다시 배포하기

```bash
git add -A
```
```bash
git commit -m "기능 수정"
```
```bash
git push
```

푸시하면 GitHub Actions가 자동으로 실행되어 몇 분 안에 사이트에 반영됩니다.

## 6. 주의 사항

1. **HTTPS가 필요한 이유**: 브라우저의 현재 위치 기능은 `https://` 또는 `localhost`에서만 동작합니다. GitHub Pages는 자동으로 HTTPS를 제공하므로 폰에서도 내 위치 기능이 정상 동작합니다.
2. **데이터는 기기별로 따로 저장됩니다**: PC에서 지인을 추가해도 폰에는 자동 반영되지 않습니다. 엑셀 내보내기/가져오기로 옮겨야 합니다. (자동 동기화가 필요해지면 계획서의 3단계 — Firebase/Supabase 도입 단계입니다.)
3. **브라우저 데이터를 지우면 사라집니다**: 정기적으로 `📤 엑셀 내보내기`로 백업하세요.
4. **저장소를 비공개로 바꾸는 경우**: GitHub Pages를 비공개 저장소에서 쓰려면 유료 플랜(GitHub Pro)이 필요합니다. 지금 구조는 데이터를 올리지 않으므로 공개 저장소로 두어도 안전합니다.
5. **Nominatim 이용 정책**: 개인적 규모의 사용을 전제로 하며, 이 앱은 초당 1건 이하로 요청합니다. 공유 앱으로 확장해 사용량이 늘어나면 유료 지오코딩 서비스로 교체해야 합니다.
